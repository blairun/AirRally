package com.air.pong.ui.components

/**
 * Enum identifying which bonus chip was clicked for popup routing.
 */
enum class BonusChipType {
    RALLY,
    GRID_UPGRADES,
    SPIN,
    COPY_CAT,
    GOLD,
    POWER_OUTAGE
}
